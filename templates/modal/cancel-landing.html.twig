<!-- Modal -->
<div class="modal fade" id="cancelLandingModal" tabindex="-1" role="dialog" aria-labelledby="cancelLandingModal" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="cancelLandingModal">Are you sure you want to CANCEL LANDING?</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <a href="{{ path('aircraft_cancel_landing', {'id':aircraft.id}) }}" class="btn btn-primary">Yes!</a>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
            <div class="modal-footer">
            </div>
        </div>
    </div>
</div>